#include<stdio.h>

int main(){
char str[30];
printf("Enter the string:");
gets(str);
// printf("%s",str);
puts(str);//puts is an unformatted output function used to print the strings and
//moves the cursor to the next line without escape sequence \n
printf("Hello");
    return 0;
    //output My name is Abdullah
}